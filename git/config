[alias]
    graph = log --abbrev-commit --all --color --decorate --graph --oneline
    st = status
    unstage = reset HEAD --
[color]
    ui = auto
[core]
    editor = vim
    ignorecase = false
    pager = less
    whitespace = blank-at-eol,blank-at-eof,tab-in-indent
[credential]
    helper = cache --timeout=3600 --socket=${HOME}/var/git-credential-cache/socket
[diff]
    tool = vimdiff
[difftool]
    prompt = false
[push]
    default = simple
[svn]
    rmdir = true
[user]
    email = enptfb55@gmail.com
    name = Sal Amato
