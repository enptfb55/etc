[alias]
    graph = log --abbrev-commit --all --color --decorate --graph --oneline
    st = status
    unstage = reset HEAD --
[color]
    ui = auto
[color "status"]
    added = green
    changed = yellow
    untracked = white
[core]
    editor = vim
    ignorecase = false
    pager = less
    whitespace = blank-at-eol,blank-at-eof,tab-in-indent
[credential]
    helper = cache --timeout=3600 --socket=${HOME}/var/git-credential-cache/socket
[diff]
    tool = vimdiff
[difftool]
    prompt = false
    tool = vimdiff
[push]
    default = simple
[pull]
    rebase = true
[svn]
    rmdir = true
[user]
	email = samato@hap-capital.com
    name = Sal Amato
